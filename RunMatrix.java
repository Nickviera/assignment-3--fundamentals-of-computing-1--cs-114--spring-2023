//import java.util.Scanner;

public class RunMatrix{
  public static void main(String[]args){
    Matrix m = new Matrix();

    m. checkPrint(); // check to see with zeros if the output is correct size
    m. printMatrix();// prints whats inside the matrix with zeros
    m. actualMatrix();//  creates maxtix using input user input
    m. printMatrix();// prints the original maxtrix
    m. flipMatrix();// flips the OG matrix
    m. printMatrix();// prints the flipped matrix
  }
}
